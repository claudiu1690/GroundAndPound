/** Stat column order for the profile XP block (must match API statProgress keys). */
export const STAT_ORDER = ["STR", "SPD", "LEG", "WRE", "GND", "SUB", "CHN", "FIQ"];

/** Maps injury severity to panel CSS modifiers. */
export const INJURY_SEVERITY_CLASS = {
  minor: "injury-minor",
  major: "injury-major",
};
