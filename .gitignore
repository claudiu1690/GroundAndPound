/node_modules/
frontend/node_modules/
frontend/dist
node_modules/

# Secrets — never commit these
.env
.env.*
!.env.example

# Per-machine Claude Code settings (agents/ and launch.json ARE shared)
.claude/settings.local.json

# Self-contained mock reviews: multi-MB HTML with every image base64-inlined.
# Regenerable, and the same screenshots are committed alongside as .jpg.
mockups/**/*.standalone.html

# Script backups (gym wipe etc.) contain fighter data
out/
